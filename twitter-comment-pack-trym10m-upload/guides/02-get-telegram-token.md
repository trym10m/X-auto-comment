# Lấy Telegram bot token và chat ID

1. Telegram -> tìm `@BotFather` -> `/newbot`.
2. Copy token bot.
3. Nhắn `hello` cho bot vừa tạo.
4. Mở: `https://api.telegram.org/botTOKEN/getUpdates`.
5. Tìm `chat.id`.
